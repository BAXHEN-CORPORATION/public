---
description: A Chip Selection component.
labels:
  ["Material UI v5", "React", "MUI 5", "Chip Button", "Chip Selection", "Tag"]
---

import { ChipSelection } from "./chip-selection";

A chip component that does have checked and unchecked state which reflects on its color on color props when checked and on default color when unchecked.

### Component usage

```js
<ChipSelection label="Hello world!" />
```

### Render hello world!

```js live
<ChipSelection label="Hello world!" color="error" />
```
